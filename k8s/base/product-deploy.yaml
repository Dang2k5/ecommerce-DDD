apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: "11"
  name: product-service
  namespace: ecommerce
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: product-service
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/restartedAt: "2025-12-21T02:39:21+07:00"
      labels:
        app: product-service
    spec:
      containers:
        - env:
            - name: SERVER_PORT
              value: "8082"
            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://pg-postgresql:5432/product_service
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  key: DB_USERNAME
                  name: ecommerce-db-secret
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: DB_PASSWORD
                  name: ecommerce-db-secret
            - name: JWT_SIGNER_KEY
              valueFrom:
                secretKeyRef:
                  key: JWT_SIGNER_KEY
                  name: identity-jwt
            - name: SPRING_JPA_HIBERNATE_DDL_AUTO
              value: update
          image: product-service
          imagePullPolicy: IfNotPresent
          name: product-service
          ports:
            - containerPort: 8082
              protocol: TCP
          resources: {}
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
